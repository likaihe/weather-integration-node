{
  "name": "hello-word-express-ts",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev -r dotenv/config --respawn --transpile-only server.ts",
    "build": "tsc",
    "start": "node -r dotenv/config dist/server.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^4"
  },
  "devDependencies": {
    "@types/express": "^4",
    "@types/node": "^20",
    "ts-node-dev": "^2",
    "typescript": "^5"
  }
}
